# =========================================================================
# 비교 도구 (testLocalvsAPI) 의존성 — 운영 코드 의존성은 제거됨.
# =========================================================================

# API 분류기 (OptimizedBatchClassifier — GPT-4.1 via RunYourAI)
langchain-openai>=0.2

# YouTube Data API v3 (댓글 fetch + 영상 검색)
google-api-python-client>=2.100

# .env 자동 로드
python-dotenv>=1.0

# Local 분류기 (KLUE-RoBERTa-large) — CUDA 12.x 의 경우 별도:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=2.1
transformers>=4.40
sentencepiece
numpy
